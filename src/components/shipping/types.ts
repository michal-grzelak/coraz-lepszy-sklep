import { type Address } from "@models/address"
import { type ShippingMethod } from "@models/shipping"

export type TShippingAddress = Address

export type TShippingMethod = { shippingMethod: ShippingMethod }
